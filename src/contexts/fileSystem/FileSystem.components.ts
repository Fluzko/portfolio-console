import styled from "styled-components";
import { Colors } from "../../globals/colors";

export const ConsoleFolder = styled.span`
  color: ${Colors.DEEP_GREEN};
`;

export const DirectoryList = styled.div`
  display: flex;
  gap: 0.25em 1em;
  flex-wrap: wrap;
`;